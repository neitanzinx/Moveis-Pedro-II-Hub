services:
  app:
    build: .
    container_name: whatsapp-bot-sistema
    restart: always # 🛡️ Garante que o bot reinicie se cair ou o servidor reiniciar
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      # Persistir a sessão do WhatsApp para não perder o QR Code ao reiniciar
      - ./.wwebjs_auth:/app/.wwebjs_auth
      - ./.wwebjs_cache:/app/.wwebjs_cache
    environment:
      - NODE_ENV=production
